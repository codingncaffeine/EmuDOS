<Window x:Class="EmuDOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EmuDOS.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="EmuDOS" Height="720" Width="1100"
        Background="#1A1411"
        AllowDrop="True" Drop="OnDrop" DragOver="OnDragOver">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Grid Grid.Row="0" Background="#2A211B">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="EmuDOS" Foreground="#E8DCC8"
                       FontSize="20" FontWeight="SemiBold" Margin="16,10"/>
            <Button Grid.Column="1" Content="Add games…" Margin="8" Padding="14,6"
                    Click="OnAddFolder" VerticalAlignment="Center"/>
        </Grid>

        <!-- The shelf: a repeating bookshelf backdrop with box covers laid on top -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.Background>
                    <ImageBrush ImageSource="/Assets/bookshelf.png" TileMode="Tile"
                                Stretch="None" AlignmentX="Left" AlignmentY="Top"
                                Viewport="0,0,561,645" ViewportUnits="Absolute"/>
                </Grid.Background>

                <ItemsControl ItemsSource="{Binding Games}" Margin="24">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="116" Height="150" Margin="10"
                                    Background="#D9B98A" CornerRadius="3"
                                    BorderBrush="#3A2A1C" BorderThickness="1">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
                                </Border.Effect>
                                <TextBlock Text="{Binding Title}" TextWrapping="Wrap"
                                           TextAlignment="Center" Foreground="#2A1B10"
                                           FontWeight="SemiBold" Margin="8"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

        <!-- Status bar -->
        <Border Grid.Row="2" Background="#2A211B">
            <Grid Margin="16,6">
                <TextBlock Text="{Binding Status}" Foreground="#C8B89C" VerticalAlignment="Center"/>
                <ProgressBar Width="120" Height="6" IsIndeterminate="True"
                             HorizontalAlignment="Right"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
